version: '2'
catalog:
  name: "gotify"
  version: "0.0.1"
  description: "Service for sending notifications"
  minimum_rancher_version: v1.6.14
  questions:
    - variable: "PRE_SHARED_KEY"
      label: "Pre shared key for api access"
      description: "This key must be shared with clients for api uasge"
      required: true
services:
  gotify:
    scale: 1
    start_on_create: true
  gotify-mock:
    scale: 1
    start_on_create: true
